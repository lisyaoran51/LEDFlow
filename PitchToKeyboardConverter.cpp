#include "PitchToKeyboardConverter.h"


PitchToKeyboardConverter::PitchToKeyboardConverter(int minP, int maxP) {
	minPitch = minP;
	maxPitch = maxP;
}

void PitchToKeyboardConverter::Convert(LightStatus* lightS, LEDStatus* ledS) {
	ledS->Add()
}